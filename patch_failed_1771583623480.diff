--- a/unknown
+++ b/unknown
@@ -1,9 +1,11 @@
 
-function errorHandler(err, req, res, next) {
-  // Current implementation likely throws or mishandles errors
-  throw new Error('error.handler');
-}
+function errorHandler(err, req, res, next) {
+  // Properly handle the error and send a safe HTTP 500 response
+  console.error('Internal Server Error:', err);
+  res.status(500).json({ error: 'Internal Server Error' });
+}
 
-// Possibly missing export or middleware registration
+// Ensure this error handler is registered as the last middleware in the app