{
    "name": "agent",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "start": "bun run src/index.ts",
        "dev": "bun run --watch src/index.ts"
    },
    "dependencies": {
        "@repo/types": "workspace:*",
        "@repo/storage": "workspace:*",
        "bun": "latest"
    },
    "devDependencies": {
        "bun-types": "latest"
    }
}