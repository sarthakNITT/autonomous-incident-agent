{
    "name": "state-service",
    "version": "1.0.0",
    "scripts": {
        "start": "bun run src/index.ts",
        "dev": "bun run --watch src/index.ts"
    },
    "dependencies": {
        "@repo/types": "workspace:*",
        "bun": "latest"
    },
    "devDependencies": {
        "@types/bun": "latest"
    }
}