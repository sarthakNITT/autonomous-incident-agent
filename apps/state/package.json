{
  "name": "state-service",
  "version": "1.0.0",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun run --watch src/index.ts"
  },
  "dependencies": {
    "@repo/types": "workspace:*",
    "bun": "latest",
    "@prisma/client": "^6.2.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prisma": "^6.2.0"
  }
}
