{
  "name": "docs-site",
  "version": "1.0.0",
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun run --watch src/server.ts"
  },
  "dependencies": {
    "@repo/types": "workspace:*",
    "bun": "latest"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}